#include "lists.h"

/**
 * is_palindrome - checks if linked list
 * is palindrome
 * @head: pointer to list
 *
 * Return: 1 if true, 0 if fase
 */
int is_palindrome(listint_t **head)
{
	if (*head == NULL)
		return (0);
	return (0);	
}
